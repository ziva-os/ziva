import pytest


# Shared fixtures can live here as the suite grows.
# Example: a Runtime instance with in-memory storage.

# @pytest.fixture
# def runtime():
#     from ziva.runtime import Runtime
#     from ziva.storage import InMemoryStorage
#     return Runtime.from_config({...}, storage=InMemoryStorage())
