id: tool.send_file
type: tool
version: 0.1.0
entry: impl.py:SendFileTool
permissions:
  fs: [read]
enabled_by_default: true
config:
  transports: [im]   # only exposed in IM sessions (feishu/telegram/wechat)
